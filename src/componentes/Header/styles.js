import styled from 'styled-components';
import { primaryColor, white } from '../../GlobalStyle';

export const Container = styled.div`
  background: ${primaryColor};
  color: ${white};
  font-weight: bold;
  text-align: center;
  padding: 20px;
  font-size: 18px;
`;
